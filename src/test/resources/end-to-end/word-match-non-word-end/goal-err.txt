Error: cannot apply --word-match to re-: it ends with non-word character '-'
  A word boundary adjacent to a non-word character requires a word character on its other side, so the search would match almost nothing.
