// No Java version check is needed here: Gradle itself refuses to run on a
// JDK older than the version that this build requires.

// Project name is read-only in build scripts, and defaults to directory name.
rootProject.name = "lookup"
